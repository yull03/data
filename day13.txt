Day13

1. 기본 클래스(Base Class)
(1) System
    System.out.println("Hello World");

    System s = new System();  (이렇게 안씀)
    out은 System의 static 변수이다.
    in은 System의 static 변수이다.

    System.currentTimeMillis()
         : 현재 시간을 밀리초 단위로 리턴
         : 1970년 1월 1일 0시 0분 0초 기준

    어디에 씀?
    변수1 = System.currentTimeMillis();
    나의 로직 수행
    변수2 = System.currentTimeMillis();
    변수2 - 변수1 = 나의 로직이 수행된 시간(ms)

    => 수행시간 측정의 방법으로 많이 쓰임

(2) StringBuffer
    String은 값이 바뀔 때마다 새롭게 공간 할당
    StringBuffer는 새롭게 할당하지 않는다.
    String은 값을 비교하는 equals()가 있으나
    StringBuffer는 없음
    대신에 StringBuffer는 toString() 메서드가 존재하여
    toString() 수행 후 나온 String으로 equals() 수행

    insert, delete, append

(3) Math
    abs(값) : 절대값(양수는 그대로, 음수는 양수로)
    ceil, floor : 올림, 내림
                    ceil(3.4) => 4
                    ceil(-3.4) => -3
    round(300.34) => 300
    무조건 정수 자리에서 반올림

    => 소수점 N자리에서 반올림하기 => 실습

    min(a, b), max(a, b) : a, b 중 최소, 최대

(4) Date와 Calendar
    날짜와 달력    





    